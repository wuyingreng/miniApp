<!-- pages/cate/cate.wxml -->
<view>
  <text>{{userInfo.name}}</text>
  <text>{{userInfo.age}}</text>
  <text>{{userInfo.gender}}</text>
  <text>{{userInfo.school}}</text>
  <button type="warn" bindtap="changeObj">修改对象属性</button>
  <view class="line"></view>
  <!-- <view wx:if="{{list && list.length > 0}}" wx:for="{{ list }}" wx:key="index">{{ item }}</view> -->
  <view wx:for="{{list}}" wx:key="index">{{ item }}</view>
  <!-- <view>{{ list[0].name }}</view> -->
  <button type="primary" bindtap="updateList">修改数组类型数据</button>
  <!-- pages/cate/cate.wxml -->
  <!-- <text>pages/cate/cate.wxml</text> -->
  <!-- 解除样式隔离 -->
  <!-- <van-button type="primary">主要按钮</van-button> -->
  <custom01 />
  <!-- 使用外部样式类 -->
  <!-- <van-button type="primary" custom-class="custom-class">主要按钮</van-button> -->
  <!-- 使用 CSS 变量：如果需要再多个页面或者一个组件中 需要批量修改组件、定制主题 -->
  <van-button type="primary" class="my-button">主要按钮0000</van-button>
</view>